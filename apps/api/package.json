{
  "name": "mynichi-api",
  "version": "0.1.0",
  "description": "mynichi API: model orchestration (translate, enrich, practice)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run dev:api",
    "dev:api": "bun --env-file=../../.env --watch src/index.ts",
    "prod:start": "bun --env-file=../../.env src/index.ts",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "dependencies": {
    "hono": "^4.11.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6"
  }
}
